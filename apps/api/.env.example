API_PORT=3001
WEB_PORT=3000

# Dev-only: skip USB printer, write+open photo and lottery ticket PNGs under tmp/receipt-previews/
# RECEIPT_PRINT_DRY_RUN=true

# Dev-only lottery overrides (mutually constrained; see README)
# LOTTERY_FORCE_OUTCOME=win
# LOTTERY_FORCE_PRIZE_ID=
