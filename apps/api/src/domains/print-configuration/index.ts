import { updatePrintConfiguration } from "./mutations/update-print-configuration";
import { getPrintConfiguration } from "./queries/get-print-configuration";

export const printConfiguration = {
  updatePrintConfiguration,
  getPrintConfiguration,
};
